# LangGraph multi-agent pipeline lives here.
#
# Planned nodes (see project TAPI, section 5.1):
#   Query Planner -> Retriever -> Extractor -> Startup Classifier
#   -> Evidence Validator -> NVIDIA RAG -> Recommendation -> Briefing
#
# Built out in a later phase once the data model and RAG pipeline exist.
